apply from: "${rootDir}/gradle/java.gradle"

configurations {
  instrumentationChecker
}

dependencies {
  compile project(':dd-java-agent:agent-bootstrap')
  compile deps.bytebuddy
  compile deps.bytebuddyagent
  compile deps.autoservice

  compileOnly project(':dd-trace-ot')

  testCompile deps.opentracing

  instrumentationChecker sourceSets.main.output
  instrumentationChecker configurations.compile
}
